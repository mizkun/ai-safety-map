# Scenario starting point and centered cards

2026-09-10

Individual scenario maps now include the existing `NOW` node at the left. A single entry connects to that node; scenarios with independent entry conditions use a branch. These introductory connections have no research edge ID, and do not add an OR claim, an AND input, or a causal assertion. The present stays outside every AND frame. The overview retains its single existing present node.

Map card titles and glossary labels are centered. Explanatory paragraphs in dialogs and the tour keep their reading layout.

The automated map checks verify one connected present per scenario, its position before all cards and frames, and its exclusion from joint conditions. Existing checks cover causal structure, node coverage, spacing and mobile viewport bounds. Browser inspection at 390 × 844 checks the English Present card, centered text and document overflow; desktop inspection checks the complete control scenario.
